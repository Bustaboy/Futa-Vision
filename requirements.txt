# Futa-Vision pinned runtime dependencies.
# Reference docs/source_document.md and CURSOR_VIBE_CODING_GUIDE.md before changing these pins.
# Python target: 3.12 for the first Gradio implementation and local-first backend.

# PyTorch stack for NVIDIA CUDA workflows used by ComfyUI/Ostris.
# If your platform requires a custom wheel index, install with the matching PyTorch index URL first,
# then re-run `python -m pip install -r requirements.txt`.
torch==2.9.1
torchvision==0.24.1
torchaudio==2.9.1

# Fast-start UI: Gradio 6.x Blocks/Tabs plus ChatInterface.
gradio==6.7.0

# Ostris AI Toolkit PyPI wrapper pin for metadata/compatibility checks.
# The preferred production path remains a Git checkout detected by setup.py.
ostris-ai-toolkit==0.2.7

# ComfyUI helper CLI for detection/install/launch orchestration.
# External ComfyUI engine target: ComfyUI v0.22.0 via `comfy install --version v0.22.0`.
comfy-cli==1.10.3

# RunPod cloud offload SDK/API wrapper.
runpod==1.9.1

# Environment/config/runtime helpers.
# Phase 0.5 keeps python-dotenv for `.env` app gating and training-engine path discovery.
python-dotenv==1.2.2
pydantic==2.12.5
pydantic-settings==2.12.0
PyYAML==6.0.3
requests==2.32.5
psutil==7.1.3
rich==14.2.0

# AI workflow helper libraries used by the future ComfyUI/Ostris client modules.
huggingface-hub==1.17.0
safetensors==0.7.0
# Pillow and opencv-python support image thumbnails, starter-image handling, neutral dataset assets, and future frame sampling.
Pillow==11.0.0
# Keep NumPy on the 1.26 LTS line for MoviePy frame processing and legacy NumPy APIs.
# NumPy 1.26.x does not support Python 3.13, so setup.py intentionally limits runtime installs to Python 3.12.
numpy==1.26.4
# OpenCV 4.12.x requires NumPy 2.x; 4.11.0.86 keeps the image/video stack compatible with NumPy 1.26.x.
opencv-python==4.11.0.86
# Phase 3.1 timeline trimming/concatenation and playable preview rendering.
# 2.2.1 is the current stable MoviePy release; pair it with NumPy 1.26.x until upstream NumPy 2.x frame processing is proven stable.
moviepy==2.2.1

# Development/test dependencies for Phase 0 / Phase 0.5 smoke tests.
# setup.py also exposes pytest through the `dev` extra for contributors who prefer extras-based installs.
pytest==9.0.1

# External engine note:
# Ostris and ComfyUI are detected as external engine checkouts because their model/custom-node
# dependency sets are GPU-specific. Keep their exact app-side pins above and use setup.py detection
# to bind this UI to an existing Pinokio/portable/manual install.

# Next step: run `python setup.py detect` to locate Pinokio/Ostris/ComfyUI paths, then copy `.env.example` to `.env`.
